import {
  calcVolumeCylinder,
  calcVolumeCuboid,
  calcVolumeSphere,
} from "./modules/calc.js.js";

console.log(calcVolumeSphere(5));

console.log(calcVolumeCylinder(5, 5));

console.log(calcVolumeCuboid(5, 5, 5));

//Add eventlistners to get values

//Add onclick to call each function

//Display results
